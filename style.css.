/* Header styling */
header {
    background-color: #2c3e50;
    color: white;
    text-align: center;
    padding: 2rem;
}
header .logo {
    max-width: 200px;
    height: auto;
    margin-bottom: 1rem;
}
header h1 {
    font-size: 2.5rem;
    margin-bottom: 0.5rem;
}
header p {
    font-size: 1.2rem;
}
/* Filter section styling */
.filter {
    max-width: 1200px;
    margin: 1rem auto;
    padding: 0 1rem;
    text-align: center;
}
.filter h2 {
    margin-bottom: 1rem;
    color: #2c3e50;
}
.filter button {
    background-color: #2c3e50;
    color: white;
    padding: 0.5rem 1rem;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    margin-right: 1rem;
}
.filter button:hover {
    background-color: #34495e;
}
.filter form {
    display: inline-block;
    margin-top: 1rem;
}
.filter input,
.filter select {
    padding: 0.5rem;
    border: 1px solid #ddd;
    border-radius: 5px;
    margin-right: 0.5rem;
    margin-bottom: 0.5rem;
    width: 150px;
}
.error {
    color: #e74c3c;
    font-size: 0.9rem;
    display: block;
    margin-top: 0.2rem;
}
.error-input,
.error-select {
    border: 2px solid #e74c3c;
    background-color: #fff5f5;
}
input:valid,
select:valid {
    border: 1px solid #ddd;
    background-color: white;
}
/* Footer styling */
footer {
    background-color: #2c3e50;
    color: white;
    text-align: center;
    padding: 1rem;
    position: fixed;
    bottom: 0;
    width: 100%;
}
/* Property card animation for smooth reset */
.property-card {
    transition: opacity 0.5s ease;
}
.property-card.fade-out {
    opacity: 0;
}
.property-card.fade-in {
    opacity: 1;
}